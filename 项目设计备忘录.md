# 学习之家 AI · 项目设计备忘录

> 用途：后续所有开发以本文件为准，避免方向漂移和幻觉。
> 最后更新：2026-09-07
> 项目路径：`E:\学习之家\`
> 前端品牌名：Atlas（AI 学习工作台）

---

## 一、项目定位

### 业务方向
**两大独立模块 + 总控规划调度**

**模块 A：代码审查助手（Code Review）**
- 用户贴代码 / 上传报错截图 → Agent 审查代码 / 诊断报错 / 给出修复建议
- 支持多模态：上传 IDE 报错截图，自动识别错误信息

**模块 B：学习路径规划（Learning Path）——独立大模块**
- 用户说想学某技术 → Agent 生成完整学习路线
- 支持多格式导出（PDF / Word / TXT / MD）
- 生成学习路线图（可视化）
- 标注学习难点
- 联网搜索各平台学习资源（视频 + 文档 + 开源项目）
- **人机交互**：生成多条路线供用户选择，支持"更多想法"

**总控**：先规划执行计划（调用哪些 Agent、并行还是串行），再按计划调度执行。

### 目标用户
开发者/学习者（项目作者自己就是目标用户，需求真实）

### 项目名
学习之家 AI

---

## 二、整体架构

```
用户输入（文字 / 图片）
  ↓
【总控 Agent】（Plan-and-Execute 模式）
  ├─ 步骤1：理解用户意图
  ├─ 步骤2：生成执行计划（调哪些 Agent、顺序、并行/串行）
  └─ 步骤3：按计划调度执行
         ┌─────────┴─────────┐
      【代码审查模块】     【学习路径模块】
         │                    ├─ 路线生成（多方案选择 + 人机交互）
         ├─ 多模态识别         ├─ 资源搜索
         ├─ 代码分析           ├─ 知识图谱可视化
         └─ 联网搜索           └─ 多格式导出
  ↓
【中间件层】（4 个钩子点）
  - 调 Agent 前
  - 调模型前
  - 调模型后
  - 调 Agent 后（含反思评估）
  ↓
【结果汇总】→ 流式输出 + 记忆存档
```

### 贯穿全程
- MCP 接入工具
- 记忆持久化（checkpointer 短期 + store 长期）
- 流式输出
- 中间件机制（日志 / 评估 / 成本统计等）
- 人机交互（Human-in-the-loop）

---

## 三、模块 A · 代码审查助手

### 能力清单
| 能力 | 优先级 | 说明 |
|------|--------|------|
| 代码 bug 检测 | P0 | 找语法错误、逻辑错误、性能问题 |
| 报错诊断（多模态） | P0 | 上传截图识别报错 + 给出修复方案 |
| 代码优化建议 | P1 | 性能优化、可读性改进、最佳实践 |
| 代码风格审查 | P1 | 命名规范、结构建议 |
| 联网搜解决方案 | P1 | MCP 搜索 Stack Overflow / 官方文档 |

---

## 四、模块 B · 学习路径规划（核心大模块）

### 核心能力（P0）
| 能力 | 说明 |
|------|------|
| 分阶段学习路线生成 | 入门 → 进阶 → 高级 → 实战 |
| 多方案路线 + 人机选择 | 生成 2-3 条不同风格路线，用户选一条 |
| 更多想法 | 用户说"再给几条"，Agent 继续生成 |
| 难点标注 | 每个阶段标出关键难点 |
| 多格式导出 | 目标 PDF / Word / TXT / MD —— **现状仅 MD + Word** |
| 学习路线图可视化 | mermaid 渲染（前端已本地化，Graphviz 未用） |
| 多平台资源搜索 | MCP Tavily 联网搜（B站 / GitHub / 官方文档等） |
| 资源质量筛选 | Agent 评估资源质量 |

### 进阶能力（P1）
| 能力 | 说明 |
|------|------|
| 个性化路线 | 根据基础/时间/风格定制 |
| 常见坑与避坑指南 | 新手最容易踩的坑 |
| 面试考点标注 | 高频考点打 ⭐ |
| 知识图谱 | 知识点依赖关系图 |

### 锦上添花（P2）
| 能力 | 说明 |
|------|------|
| 学习进度追踪 | 打卡、记住学到哪了 |
| 卡壳求助 | 学到某点卡住了直接问 |
| 路线动态调整 | 根据学习反馈调整难度 |
| 学习计划表（日历） | 把每天学什么排进日历 |
| 分享卡片 | 生成漂亮的路线图图片 |
| 职业路径导向 | 目标岗位 → 学习路线 + 招聘要求 |

### 两个模块的联动（可选）
- 代码审查发现用户某个知识点薄弱 → 自动推荐对应学习路径片段
- 联动不是融合，两个模块还是独立的

---

## 五、技术选型（已确认）

| 层 | 技术 | 说明 |
|----|------|------|
| **Agent 框架** | LangChain v1 `create_agent` | 新标准 API |
| **工作流编排** | LangGraph + Plan-and-Execute | 规划先行 + 串行/并行混合调度 |
| **多模态模型** | 通义千问 Qwen-VL-Max | 阿里云 DashScope 调用 |
| **文本模型** | DeepSeek | 处理规划/审查/反思等文字任务 |
| **工具接入** | MCP 协议 | `langchain-mcp-adapters` |
| **记忆** | checkpointer（短期）+ store（长期） | |
| **后端** | FastAPI | 已完成，端口 8000 |
| **前端** | 原生 HTML/CSS/JS（Atlas） | 已完成，vendor 本地化 |
| **部署** | Docker | **未开始**（缺 Dockerfile 和依赖清单） |
| **文档导出** | python-docx（reportlab 未引入） | Word ✅ / PDF ❌ |
| **可视化** | mermaid（Graphviz 未引入） | 前端 vendor 本地渲染 |

### 新增核心机制
1. **规划先行（Planner-first）**：总控先生成执行计划，再调度
2. **并行 + 串行混合调度**：执行计划可标记并行，LangGraph fan-out/fan-in
3. **中间件机制**：4 个钩子点（调Agent前/后、调模型前/后）
4. **人机交互（Human-in-the-loop）**：`interrupt` 中断，等用户选择后继续

### 上一个项目已掌握的能力，直接复用
- RAG 全链路
- MCP 工具接入
- LangGraph ReAct 单 Agent
- Redis 短期记忆缓存
- Docker / docker-compose 部署
- FastAPI + SSE 流式输出

### 本项目新增的能力（重点学习目标）
- `create_agent` 新标准 API
- 多 Agent 协作 + 规划先行 + 并行调度
- 中间件机制（4 个钩子）
- 人机交互（Human-in-the-loop）
- 反思评估闭环（5 维度 + LLM-as-a-Judge）
- 多模态理解（Qwen-VL）
- 记忆持久化（checkpointer + store）
- 文档多格式导出
- 知识图谱 / 路线图可视化

---

## 六、反思评估闭环（中间件的一部分，挂在"调 Agent 后"）

### 评估维度（5 个，每个 1-5 分）
1. **Accuracy（准确性）**：事实对不对？
2. **Completeness（完整性）**：有没有遗漏？
3. **Clarity（清晰度）**：表达清楚吗？
4. **Helpfulness（有用性）**：能解决问题吗？
5. **Safety（安全性）**：有没有错误/有害内容？

### 打回规则
- 任一维度 < 3 分 → 打回
- 总分 < 20 分 → 打回
- 最多打回 3 次

---

## 七、多 Agent 分工

| Agent | 职责 | 模型 | 所属模块 | 状态 |
|-------|------|------|---------|------|
| **总控（Supervisor）** | 调度 + 汇总结果 | DeepSeek | 全局 | ✅ |
| **多模态识别 Agent** | 识别图片中的代码/报错 | Qwen-VL | 代码审查 | ✅ |
| **代码审查 Agent** | 分析代码、找 bug、给建议 | DeepSeek | 代码审查 | ✅ |
| **搜索 Agent** | 联网搜解决方案/资源 | DeepSeek + MCP | 共用 | ✅ |
| **学习规划 Agent** | 生成路线、难点、图谱 | DeepSeek | 学习规划 | ✅ |
| **导出模块** | 生成 MD / Word（PDF·TXT 未做） | 纯代码，无 LLM | 学习规划 | ✅ |
| **澄清 Agent（clarify）** | 需求宽泛时 interrupt 抛 A/B/C 选项 | 纯代码，无 LLM | 全局 | ✅ |
| **输入重写器（rewriter）** | 短句补全成完整问题（总控前） | DeepSeek（便宜） | 全局 | ✅ |

> 说明：导出和澄清是"工具/模块"而非 Agent——导出是纯格式转换，澄清只是 interrupt 包装，都不需要模型推理，别按 Agent 去找它们。

---

## 八、实施顺序（一步一确认）

```
第1层   基础设施（config + llm 模型工厂）        ✅ 已完成
第2层   总控规划调度骨架（create_agent ReAct）    ✅ 已完成
第3层   中间件框架（日志 / 记忆 / 反思）          ✅ 已完成
第4层   代码审查模块核心                          ✅ 已完成
第5层   学习路径模块核心（路线生成 + 难点标注）    ✅ 已完成
第6步   MCP 接入工具                              ✅ 已完成
第7步   并行调度（fan-out/fan-in）                ⏸ 已确认暂缓，不做
第8步   人机交互（interrupt + 用户选择）          ✅ 已完成
第9步   反思评估闭环（接入中间件）                ✅ 已完成
第10步  多模态接入（Qwen-VL）                     ✅ 已完成
第11步  学习路径进阶（导出 + 可视化 + 资源搜索）   ✅ 已完成（PDF/TXT 未做）
第12步  记忆持久化                                ✅ 已完成
第13步  FastAPI 后端 + 前端                       ✅ 已完成
第14步  Docker 部署                               ❌ 未开始
```

### 当前进度备注（2026-09-07 更新）

- **第8步 人机交互**：已落地。`src/modules/clarify/agent.py` 用 `interrupt(ClarifyRequest)` 暂停图，抛出 A/B/C 选项卡片，用户选择后 `Command(resume=...)` 恢复。
  - 注意：总控提示词后续已调整为"默认直接干，不要澄清"——只有同时满足"关键信息缺失 / 无合理默认值 / 猜错代价大"三条才发起澄清，最多 2 轮。这是有意的收窄，不是功能缺失。
- **第10步 多模态**：已落地。`src/modules/multimodal/`（qwen_vl.py 195 行 + agent.py），前端支持点击上传和拖拽上传，走 `/api/upload/image`。
- **第11步 学习路径进阶**：
  - 导出 ✅：`src/modules/learning_path/exporter.py`，**目前只支持 Markdown(.md) 和 Word(.docx)**。PDF / TXT **未实现**（备忘录里写的四种格式是目标，不是现状）。
  - 可视化 ✅：前端 vendor 本地化 `mermaid.min.js`，回答里的 mermaid 代码块自动渲染成图；`highlight.min.js` 负责代码高亮，`marked.min.js` 负责 Markdown 解析。
  - 资源搜索 ✅：走 MCP Tavily，总控提示词里要求附真实可访问链接，搜不到就如实说明，不许编造课程名。
- **第12步 记忆持久化**：已落地，三层各管一摊。
  - `data/checkpoints.db` — LangGraph checkpointer，会话状态 / interrupt 暂停点（短期）
  - `data/memory.db` — `src/memory/store.py`，跨会话长期记忆，namespace + key 存 JSON
  - `data/messages.db` — `src/memory/messages.py`，对话历史，供输入重写器读上文
  - `data/conversations.db` — 前端会话列表（新建 / 重命名 / 删除 / 加载）
- **第13步 后端 + 前端**：已落地并可用。
  - 后端：`src/api/app.py` + `routes/chat.py`(729行) + `routes/workspace.py`。SSE 流式走 `/api/chat/stream`，另有 `/api/export`、`/api/upload/image`、`/api/conversations*`、`/api/workspace*`。
  - 前端：`frontend/`（index.html 149 + app.js 605 + style.css 697），品牌名 Atlas。**vendor 库已全部本地化，不依赖 CDN**。
  - 启动：`python run_api.py`（uvicorn :8000），前端访问 `/static/index.html`。
  - 工作空间：用户可在前端选导出目标文件夹，路径存 `data/workspace.json`。
- **第14步 Docker**：**未开始**。项目里没有 Dockerfile / docker-compose.yml / requirements.txt / pyproject.toml，依赖清单缺失，是部署前必须补的坑。

#### 代码现状速查（2026-09-07）

| 目录 | 说明 |
|------|------|
| `src/config.py` | 配置中心：DeepSeek + DashScope(Qwen-VL) + Tavily，从 `.env` 读 |
| `src/agents/base.py` | 子 Agent 基类（638 行），中间件挂载、反思评估都在这 |
| `src/agents/supervisor.py` | 总控 ReAct（465 行），`astream_supervisor` 里做了内部 JSON 吞噬过滤 |
| `src/agents/middleware/` | logging / memory / reflection 三个中间件 |
| `src/agents/checkpointer.py` | SQLite checkpointer 异步上下文 |
| `src/modules/` | code_review、learning_path(+exporter)、search、multimodal、clarify、rewriter、tools |
| `src/modules/rewriter/` | 输入重写器：≤15 字的短句 + 有历史 → 补全成完整问题再喂总控（解决"用户回一个 D"） |
| `src/modules/tools/` | registry(注册中心) + mcp_client + agent_tools + init_tools |
| `src/api/routes/chat.py` | 核心接口（729 行），SSE 流式 + 导出 + 上传 |
| `src/memory/` | messages(短期对话) + store(长期记忆) |
| `frontend/` | Atlas 前端，vendor 本地化 |
| `data/` | 4 个 SQLite + exports/ + uploads/ + workspace.json |

规模：43 个 .py 文件约 4600 行，前端约 1450 行。

#### 已知遗留问题

1. **根目录 26 个 `_test_*.py` 调试脚本** + `ceshi.py` / `test.py` / `_inspect_conv.py`，全是开发期临时验证用的，待清理。
2. `main.py` 还是 PyCharm 模板文件（print_hi），没用到，可删。
3. `src/api/routes/chat.py` 里有"临时诊断日志"（写 `data/debug_chat.log`），注释说排查完删掉，但还在。
4. 缺少依赖清单和安装说明（requirements.txt / pyproject.toml）。
5. git 只有 1 个提交（7531d7d），无过程记录。

---

### 历史进度备注（2026-08-24）

- **第6步 MCP 接入工具**：已落地并验证通过（A方案：共享工具层 + 按需授权）。
  - SearchAgent 内部工具已从手写 Tavily SDK 换成 MCP 工具（`mcp_tavily` 搜索 + `mcp_playwright` 抓取），实现异步懒加载 `init_mcp_tools()`
  - 总控 Supervisor 只管调度（持有 3 个子 Agent 工具），不直接持 MCP 工具
  - 已删除死代码 `src/modules/search/tools.py`
- **第7步 并行调度**：**暂缓不实施**（经讨论确认）。
  - 旧版 StateGraph 时代实现过（ExecutionPlan.mode 字段 + gather）；改造为 create_agent 后未实现
  - 当前 create_agent 总控是 ReAct 串行；子 Agent 已全异步（具备并发地基）
  - 未来如需，方案：总控外层用 `asyncio.gather` 并发调用多个子 Agent 的 `arun()`、`await` 等最慢返回，用确定性 Python 合并；`ExecutionPlan.mode == "parallel"` 作为开关。需用户明确要求再做
- **第9步 反思评估闭环**：已接入并生效（５维度 LLM-as-a-Judge + 打回重跑）。

---

## 九、约束与注意事项

> ⚠️ **硬性规则**：每进行一个新步骤/新文件之前，必须先重新翻看本备忘录，防止方向偏离和幻觉。

1. **一步一确认**：每个阶段完成并经用户确认后，才进入下一阶段
2. **文件存放**：所有项目文件放在 `E:\学习之家\` 下
3. **虚拟环境**：`E:\学习之家\.venv`（Python 3.13.2）
4. **两大模块独立**：总控规划调度，不强行融合
5. **真实原则**：已落地的写"熟悉"，进行中的写"进行中"
6. **每完成一个大步骤，重新翻看本备忘录**，防止幻觉
7. **Python 是主力语言**
8. **修改文件必须经用户明确同意**，只能自动检查，不能自动修改

---

## 十、历史项目参考

### 上一个项目：萌宠之家 AI 客服系统
- 技术栈：LangGraph ReAct + RAG 混合检索 + MCP + Redis + Docker
- GitHub：https://github.com/zhyzhy1122/zhyyy
- 位置：`D:\python\宠物店rag项目（后续langchain加langgraph）`
